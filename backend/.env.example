# Local development example:
DATABASE_URL=sqlite:///./rootslink_dev.db

# In Render, replace DATABASE_URL with the PostgreSQL connection string.
# Restrict this to the exact GitHub Pages URL after deployment:
CORS_ORIGINS=https://YOUR-GITHUB-USERNAME.github.io
